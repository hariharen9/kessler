✅ COMPLETED


  Pillar 3: Flawless User Experience (The "TUI")
   * ✅ Sorting & Filtering: Press `s` to sort by Size/Name. Press `/` to search and filter projects dynamically.
   * ✅ Tier Toggle (Safe vs. Deep Clean): Press `t` to toggle between Safe (100% regeneratable) and Deep (binaries/builds). Header updates in red/green.
   * ✅ Orbital Telemetry sidebar: Disk usage, size charts, breakdown by ecosystem.
   * ✅ Select All: Press `a` to select/deselect all filtered projects.
   * ✅ Tab-Based Navigation: Press `1`/`2`/`3` or `Tab` to switch between Projects, Global Caches, and History tabs.
   * ✅ Live Scanning Screen: ASCII art box with real-time stats (dirs checked, projects found, debris size, latest project) and rotating space quotes. Shows last scan from history.
   * ✅ Size Sparklines: Inline relative size bars (████░░░░) next to each project and global cache.
   * ✅ Project Path Tooltip: Full path of the selected project displayed above the help bar.
   * ✅ Preview Pane (Height-Constrained): Preview pane truncates to fit terminal height, shows "+N more items (press p to expand)". Press `p` to toggle a full-screen modal with all artifacts.
   * ✅ Nuke Confirmation Modal: Pressing `X` shows a centered red ☢️ modal with item count, size, and y/n prompt before permanent deletion.
   * ✅ Post-Clean Re-scan: After cleaning (trash or nuke), TUI re-scans and returns to results with a green "✨ Freed X GB" banner instead of exiting.
   * ✅ Scan History: Past 20 scans saved to `~/.config/kessler/history.json`. Viewable in the History tab (Tab 3) with timestamp, path, projects, debris, and freed space.
   * ✅ Non-Interactive / CI Mode: `kessler scan` and `kessler clean` subcommands with `--json`, `--dry-run`, `--force`, `--permanent`, `--min-size`, `--older-than`, `--include-ignored`, and `--sort` flags.
   * ✅ Custom User Rules: Engine merges `~/.config/kessler/rules.yaml` with embedded defaults. Rules merged by name, triggers/targets appended, danger zone unioned.


  Pillar 4: Distribution & Platform Support
   * ✅ Cross-Platform Trash API: Replaced hardcoded macOS ~/.Trash with platform-specific wrappers. macOS/Linux use `go-trash`, Windows uses PowerShell COM (CGO-free).
   * ✅ GoReleaser Integration: Builds for macOS/Linux/Windows (amd64+arm64), DEB packages, Homebrew formula, Scoop manifest. GitHub Actions triggers on `v*` tag push.
   * ✅ Homebrew Formula: Auto-generated → `hariharen9/homebrew-tap`. Install: `brew tap hariharen9/tap && brew install kessler`.
   * ✅ Scoop Bucket: Auto-generated → `hariharen9/scoop-bucket`.
   * ✅ AUR Package: PKGBUILD for Arch Linux (`yay -S kessler-bin`).
   * ✅ npm Wrapper: Published as `kessler-cli` on npmjs.com. Install: `npm install -g kessler-cli` or `npx kessler-cli`.
   * ✅ DEB Package: Generated by GoReleaser for Debian/Ubuntu.


  Pillar 1: Advanced Intelligence (The "Brain")
   * ✅ Dynamic `.gitignore` Parsing: Queries Git for ignored directories. Labeled "[user ignored]" in yellow. Toggle with `i` key or `--include-ignored`. Only directories surfaced, never individual files.
   * ✅ Time-Based "Cold Storage" Filtering: `--older-than` flag (e.g., `kessler scan --older-than 30d`). Available on `scan` and `clean`.


  Pillar 2: Global Scope (The "Everywhere" Clean)
   * ✅ Package Manager Global Caches (Tab 2): Scans npm, Yarn, pnpm, pip, Go modules, Gradle, Cargo, CocoaPods, and Homebrew system caches. Shows sizes, sparklines, and equivalent CLI commands. Uses native clean commands when available, with directory removal fallback.
   * ✅ Global Clean Confirmation: Always shows a yellow ⚠️ confirmation modal listing selected caches, sizes, and CLI equivalents before cleaning.
   * ✅ Safety Caution Banner: "Global caches are shared system resources. Deleting them affects ALL projects." displayed at the top of the Global tab.
   * ✅ Docker & Container Debris: Parses `docker system df` for reclaimable space. Cleans with `docker system prune -f` (dangling images, stopped containers, build cache). Only shown if Docker is installed.
   * ✅ IDE & OS Junk: Xcode DerivedData, JetBrains IDE caches (IntelliJ/WebStorm/PyCharm). Cross-platform paths.

  ---


  ⏳ PENDING / TO DO


  Pillar 1: Advanced Intelligence (The "Brain")
   * [ ] Monorepo & Symlink Safety: Detect pnpm-workspace.yaml, lerna.json, etc., to avoid symlink loops or double-counting workspace dependencies.

  Pillar 2: Global Scope
   * [ ] Multi-root scan: Pass multiple paths (e.g., `kessler ~/Projects ~/Work ~/Side-projects`).

  Pillar 3: TUI
   * [ ] Multi-root scan: Pass multiple paths (e.g., `kessler ~/Projects ~/Work ~/Side-projects`).