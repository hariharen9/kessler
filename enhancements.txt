✅ COMPLETED


  Pillar 3: Flawless User Experience (The "TUI")
   * ✅ Sorting & Filtering: You can now press s to sort the project list by Size or Name. You can press / to open a search bar and dynamically filter the list by typing (e.g., "react").
   * ✅ Tier Toggle (Safe vs. Deep Clean): You can now press t to instantly toggle between Safe (100% regeneratable caches like node_modules) and Deep (compiled binaries/builds). It also visually updates the header
     in red/green to indicate the mode.
   * (Bonus Addition): We added an Orbital Telemetry sidebar that wasn't strictly on the list but significantly elevates the TUI experience by showing disk size, usage charts, and size breakdown by ecosystem.
   * (Bonus Addition): We added the a hotkey to instantly "Select All" currently filtered/visible projects.


  Pillar 4: Distribution & Platform Support
   * ✅ Cross-Platform Trash API: We completely replaced the hardcoded macOS ~/.Trash logic with github.com/laurent22/go-trash. The tool now natively supports moving items to the Windows Recycle Bin, Linux
     FreeDesktop Trash, and macOS Trash. We also added an interactive fallback prompt if cross-drive trashing fails.

  ---


  ⏳ PENDING / TO DO


  Pillar 1: Advanced Intelligence (The "Brain")
   * ✅ Dynamic `.gitignore` Parsing: Kessler queries Git for ignored directories within each project. Custom-ignored artifacts are labeled as "[user ignored]" in the dashboard with a yellow tag. Users toggle visibility with the `i` key in the TUI or `--include-ignored` in CLI. Individual files (.env, lockfiles) are never surfaced — only directories.
   * ✅ Time-Based "Cold Storage" Filtering: The `--older-than` flag (e.g., `kessler scan --older-than 30d`) filters projects by last modified date. Available on both `scan` and `clean` subcommands.
   * [ ] Monorepo & Symlink Safety: Specifically detect pnpm-workspace.yaml, lerna.json, etc., to avoid infinitely looping through symlinks or double-counting massive workspace dependencies.


  Pillar 2: Global Scope (The "Everywhere" Clean)
   * [ ] Package Manager Global Caches: Add support to scan system-level hidden folders (e.g., ~/.npm, ~/.cache/pip, ~/go/pkg/mod).
   * [ ] Docker & Container Debris: Integrate docker system prune logic.
   * [ ] IDE & OS Junk Extensions: Add deeper rules for Xcode DerivedData and Android Studio .gradle caches.


  Pillar 3: Flawless User Experience (The "TUI") (Remaining items)
   * ✅ Non-Interactive / CI Mode: Implemented `kessler scan` and `kessler clean` subcommands with Cobra. Supports `--json`, `--dry-run`, `--force`, `--permanent`, `--min-size`, `--older-than`, `--include-ignored`, and `--sort` flags.
   * ✅ Custom User Rules: The engine merges `~/.config/kessler/rules.yaml` with the embedded defaults. Rules are merged by name, triggers/targets are appended, danger zone entries are unioned.


  Pillar 4: Distribution & Platform Support
   * ✅ GoReleaser Integration: `.goreleaser.yaml` builds for macOS/Linux/Windows (amd64+arm64), generates DEB packages, Homebrew formula, and Scoop manifest. GitHub Actions triggers on `v*` tag push.
   * ✅ Homebrew Formula: Auto-generated by GoReleaser → `hariharen/homebrew-tap`. Users install via `brew tap hariharen/tap && brew install kessler`.
   * ✅ Scoop Bucket: Auto-generated by GoReleaser → `hariharen/scoop-bucket`. Windows users install via `scoop install kessler`.
   * ✅ AUR Package: PKGBUILD for Arch Linux (`yay -S kessler-bin`).
   * ✅ npm Wrapper: Thin npm package that downloads the correct binary. Users can `npx kessler` or `npm install -g kessler`.
   * ✅ DEB Package: Generated by GoReleaser for Debian/Ubuntu (`sudo dpkg -i kessler_*.deb`).